/* A way to represent a boolean value using 1 bit! */

#include <stdio.h>

union boolean {
	unsigned bool : 1;
};

typedef union boolean boolean;

int main(void)
{
	boolean b;
	b.bool = 0;
	if (b.bool) {
		printf("true\n");
	}
	else {
		printf("false\n");
	}
	return 0;
}
