#include <stdio.h>

union boolean {
	unsigned bool : 1;
};

typedef union boolean boolean;

int main(void)
{
	boolean b;
	b.bool = 1;
	if (b.bool) {
		printf("true\n");
	}
	else {
		printf("false\n");
	}
	return 0;
}
