/* Header file for useful functions in wordScrambler. */

#include <stdio.h>
#include <stdlib.h>

int is_alphanumeric(char c);
int is_lowerCase(char c);
int is_upperCase(char c);
int is_numeric(char c);
