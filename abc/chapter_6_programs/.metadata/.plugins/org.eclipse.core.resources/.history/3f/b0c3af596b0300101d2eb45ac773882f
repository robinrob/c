/* Some more uses of pointers and arithmetic exercises.
 * main.c
 *
 *  Created on: Dec 9, 2010
 *      Author: user
 */

char *format = "%p %d %d %d\n";
int i = 3;
int *p = &i;

printf(format, p, *p + 7, 3 * **&p + 1, 5 * (p - (p - 2)));
