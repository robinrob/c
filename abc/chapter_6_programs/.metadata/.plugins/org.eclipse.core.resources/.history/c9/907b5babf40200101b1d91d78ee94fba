/*
 * fct.c
 *
 *  Created on: Dec 8, 2010
 *      Author: user
 */

#include "find_root.h"

dbl f(dbl x, int order)
{
	double value = 0.0;
	int i, j;

	for (i = 1; i < order; ++i) {
		for (j = 1; j < i; ++j) {

		}
	}

	return (x * x * x * x * x - 7.0 * x - 3.0);
}
