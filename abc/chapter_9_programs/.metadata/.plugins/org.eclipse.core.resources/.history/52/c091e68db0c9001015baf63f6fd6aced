//Does the system implement signed arithmetic for int bit fields?

struct test {
	int a : 3, b : 4;
} x = {0};

int main(void)
{
	for ( ; ; ) {
		printf("x.a = %2d, x.b = %2d", x.a++, x.b++);
	}
	return 0;
}
