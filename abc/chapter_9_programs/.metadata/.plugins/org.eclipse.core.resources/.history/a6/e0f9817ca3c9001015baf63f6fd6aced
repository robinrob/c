//Header file for bcd project.

#include <limits.h>
#include <stdio.h>

typedef struct {
	unsigned a: 4, b: 4, c: 4, d: 4, e: 4, f: 4, g: 4, h: 4;
} bcd;

void bit_print(bcd a);
void print_bcd(bcd *a);
