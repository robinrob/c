/* Bit print an expression. */

#include "bcd.h"

void bit_print(int a, int n_bits)
{
	int i;
	int mask = 1 << (n - 1);
	
	for (i = 1; i <= n; ++i) {
		putchar(((a & mask) == 0) ? '0' : '1');
		a <<= 1;
		if (i % (CHAR_BIT) == 0 && i < n) {
			putchar(' ');
		}
	}
	printf("\n");
}
