//Binary Coded Decimal implementation

#include "bcd.h"

int main(void)
{
	bcd number = {1, 2, 3, 4, 5, 6, 7, 8};
	//number.a = 12;

	dec_print_bcd(number);
	bit_print_bcd(number);

	bcd_to_bin(number);

	return 0;
}

/* Prints the value of a binary coded decimal variable
 * as a decimal.
 */
void dec_print_bcd(bcd a)
{
	printf("bcd: %d%d%d%d%d%d%d%d\n",
			a.a, a.b, a.c, a.d,
			a.e, a.f, a.g, a.h);
}

//Bit-prints the value of a binary coded decimal variable.
void bit_print_bcd(bcd a)
{
	printf("Bit-printed bcd:\n");
	bit_print(a.a, 4);
	bit_print(a.b, 4);
	bit_print(a.c, 4);
	bit_print(a.d, 4);
	bit_print(a.e, 4);
	bit_print(a.f, 4);
	bit_print(a.g, 4);
	bit_print(a.h, 4);
}

//Converts a BCD to a decimal number and returns it as an int.
int bcd_to_dec(bcd a)
{
	int dec = 0, power = 10, i;
	for (i = 0; i < 7; ++i) {

	}

	dec += a.a;
	return 0;
}
