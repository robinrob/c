/* Header file for counting functions. */

#include "sllist.h"

/* Counts the length of a list recursively. */
int cnt_list(NODE *list);

/* Counts the length of a list iteratively. */
int cnt_list_it(NODE *list);
