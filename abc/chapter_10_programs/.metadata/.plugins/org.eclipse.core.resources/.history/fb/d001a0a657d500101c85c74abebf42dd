/* Main function for Doubly-Linked List project. */

#include "dllist.h"

int main(void)
{
	NODE *list = create_list(create_array5(), 5);

	list
//	print_test(list);
	ins_del_test(list);
//	del_dupl_test(list);

	return 0;
}
