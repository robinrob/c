/* Creating a linked list from an array. */

#include "sllist.h"

int main(void)
{
	DATA *d;
	NODE *list;

	d = create_array5();
	list = create_list(d, 5);

	print_test(list);
	sort_test(list);
	cycle_test(list);
	concat_test(list);
	ins_del_test(list);

	free(list);

	return 0;
}






