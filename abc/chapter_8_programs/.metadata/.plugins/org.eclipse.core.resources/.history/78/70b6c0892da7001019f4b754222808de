/*
 * main.c
 *
 *  Created on: 5 Jul 2011
 *      Author: robin
 */


#include "sort.h"

int main(void)
{
	int a[100], i;
	srand(time(NULL));
	for (i = 0; i < 100; ++i) {
		a[i] = rand() % 100);
	}
	int *p = partition()
}
