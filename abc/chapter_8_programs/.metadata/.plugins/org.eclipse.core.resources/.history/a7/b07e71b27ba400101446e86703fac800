/*Program to sort a list of words using quicksort.*/

#include "sort.h"

#define MAX_LIST 50

int main(void)
{
  char *a[MAX_LIST];
  int i = 0;
  while (scanf("%s", a + i) == 1) {
    ++i;
  }

  printf("List before sort:\n");
  print_list(a, a + n - 1);;
  //printf("\nQuicksorting ...\n");
  //quicksort(a, a + n - 1);
  //print_list(a, a + n - 1);
  
  return 0;
}
