#include "g_lib.h"
#include <ctype.h>

int main(void)
{
    char *s = gcalloc(20, sizeof(char));
    s = "Hello Robin\n";
    printf("%s", s);
    free(s);
    FILE *file = gfopen("out.txt", "w");
    fprintf(file, "Hello Robin\n");
    fclose(file);
    return 0;
}
