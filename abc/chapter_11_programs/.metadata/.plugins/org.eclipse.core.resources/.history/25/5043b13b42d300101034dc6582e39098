#include "g_lib.h"
#include <ctype.h>

int main(void)
{
    char *s = gcalloc(5, sizeof(char));
    s = "Robin";
    printf("%s\n", s);
    free(s);
    FILE *file = gfopen("test.txt", "w");
    fprintf("Hello Robin\n");
    fclose(file);
    return 0;
}
