#include "g_lib.h"

int main(void)
{
    char *s = gcalloc(5, sizeof(char));
    s = 'R', s + 1 = 'o', s + 2 = 'b', s + 3 = 'i', s + 4 = 'n';
    printf("%s\n", s);
    free(s);
    FILE *file = gfopen("test.txt", "w");
    fprintf("Hello Robin\n");
    fclose(file);
    return 0;
}
