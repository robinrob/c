#include "g_lib.h"
#include <ctype.h>

int main(void)
{
    char *s = gcalloc(5, sizeof(char));
    s = "Hello Robin\n";
    printf("%s", s);
    free(s);
    FILE *file = gfopen("test.txt", "w");
    fprintf("Hello Robin\n");
    fclose(file);
    return 0;
}
