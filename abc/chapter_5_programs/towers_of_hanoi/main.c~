/*
 * main.c
 *
 *  Created on: Jan 22, 2010
 *      Author: user
 */

#include "hanoi.h"

int cnt = 0;

int main(void)
{
	int n;

	n = get_n_from_user();
	assert(n > 0);
	move(n, 'A', 'B', 'C');
	printf("\nTotal number of moves: %d\n", cnt);
	return 0;
}
