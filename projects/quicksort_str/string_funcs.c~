#include "sort.h"

void swap_string(char **p, char **q)
{
  char *c = *p;
  *p = *q;
  *q = c;
}

void print_list(char **a, int n)
{
  int i;
  for (i = 0; i < n; ++i) {
    printf("%s\n", *(a + i));
  }
}